# 2026-07-25

- Created GitHub account, connected it via Composio.
- Set up this repo (`homesoc-lab-alex`) with `docs/`, `notes/`, `journal/` structure, mirroring Tyler's `homesoc-lab` repo.
- Confirmed Kali Linux installer ISO already present on `SV1` (shared Proxmox host) local storage.

**Next up:** create VM 106 (`kali-attacker-alex`) via the Proxmox Create VM wizard, then VM 107 (`metasploitable-alex`). Still need to confirm the `10.10.20.x` IP range with Tyler before first boot.
