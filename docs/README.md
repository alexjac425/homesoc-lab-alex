# docs/

Finished writeups go here once each piece of the lab is actually built: VM creation steps, network/IP configuration, Security Onion integration notes (from the shared side), and any config decisions worth remembering later.

Nothing here yet — first writeup will be the Kali VM (106) build once it's created.
